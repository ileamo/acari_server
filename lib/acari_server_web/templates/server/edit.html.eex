<h1>Редактировать сервер</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.server_path(@conn, :update, @server)) %>

<span><%= link "К списку", to: Routes.server_path(@conn, :index) %></span>
