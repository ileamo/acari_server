<h1>Список Пользователей</h1>
<table id="datatable" class="table display table-striped table-bordered table-sm">
  <thead>
    <tr>
      <th>Username</th>
      <th>Is admin</th>

      <th scope="col"></th>
    </tr>
  </thead>
  <tbody>
<%= for user <- @users do %>
    <tr>
      <td><%= user.username %></td>
      <td><%= user.is_admin %></td>

      <td>
        <%= link "Показать", to: Routes.user_path(@conn, :show, user) %>
        <%= link "Редактировать", to: Routes.user_path(@conn, :edit, user) %>
        <%= link "Удалить", to: Routes.user_path(@conn, :delete, user), method: :delete, data: [confirm: "Are you sure?"] %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<span><%= link "Новый пользователь", to: Routes.user_path(@conn, :new) %></span>
