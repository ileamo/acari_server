<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <link rel="SHORTCUT ICON" href="/images/bogatka-900.png">
    <title>Богатка</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
  </head>


  <body class="text-center">

    <dev class="signin">
      <%= form_for @changeset, @action, [class: "form-signin", autocomplete: "off"], fn f -> %>
       <!-- <img class="mb-4" src="<%= Routes.static_path(@conn, "/images/nsg_logo.svg") %>" alt="" width="72" height="72">

       <img class="mb-4" src="<%= Routes.static_path(@conn, "/images/bogatka.png") %>" alt="" width="128" height="72"> -->

        <h1 class="h3 mb-3 font-weight-normal">Богатка</h1>

        <div class="form-group">
          <%= label f, :username, class: "control-label sr-only" %>
          <%= text_input f, :username, class: "form-control", placeholder: "Имя", autocomplete: "nope" %>
          <%= error_tag f, :username %>
        </div>

        <div class="form-group">
          <%= label f, :password, class: "control-label sr-only" %>
          <%= password_input f, :password, class: "form-control", placeholder: "Пароль", autocomplete: "new-password" %>
          <%= error_tag f, :password %>
        </div>

        <div class="form-group">
          <%= submit "Войти", class: "btn btn-primary btn-lg btn-block" %>
        </div>
        <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
        <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
        <p class="mt-5 mb-3 text-muted">&copy; NSG 2018-<%= NaiveDateTime.utc_now().year %></p>

      <% end %>
    </dev>
  </body>

</html>
